# Доклад: Как работает Assessment в Talim AI

## 📋 Содержание

1. [Обзор системы](#обзор-системы)
2. [Архитектура](#архитектура)
3. [Жизненный цикл Assessment](#жизненный-цикл-assessment)
4. [Детальный процесс тестирования](#детальный-процесс-тестирования)
5. [API Endpoints](#api-endpoints)
6. [Использование OpenAI](#использование-openai)
7. [Структура данных](#структура-данных)
8. [Особенности реализации](#особенности-реализации)

---

## 🎯 Обзор системы

**Talim AI** - это система AI-тестирования профессиональных компетенций через голосовое собеседование.

### Ключевые возможности:

- ✅ **Адаптивное тестирование** - вопросы генерируются динамически на основе предыдущих ответов
- ✅ **Голосовое взаимодействие** - пользователь отвечает голосом, аудио транскрибируется через Whisper
- ✅ **AI-оценка** - ответы оцениваются через GPT-4 с выявлением пробелов в знаниях
- ✅ **Персональные roadmaps** - автоматическая генерация планов развития на основе результатов

### Технологический стек:

- **Backend**: Python 3.11+, FastAPI
- **Database**: PostgreSQL (Supabase)
- **AI**: OpenAI API (GPT-4-turbo-preview, Whisper-1)
- **Authentication**: JWT токены

---

## 🏗️ Архитектура

```
┌─────────────┐
│   Frontend  │ (React)
└──────┬──────┘
       │ HTTP/REST
       │ JWT Auth
       ▼
┌─────────────────────────────────────┐
│         FastAPI Backend             │
│  ┌──────────────────────────────┐  │
│  │   API Endpoints              │  │
│  │  - /api/assessments          │  │
│  │  - /api/questions            │  │
│  │  - /api/roadmaps             │  │
│  └───────────┬──────────────────┘  │
│              │                      │
│  ┌───────────▼──────────────────┐  │
│  │   Services Layer             │  │
│  │  - AssessmentService         │  │
│  │  - OpenAIService             │  │
│  │  - SupabaseService           │  │
│  └───────────┬──────────────────┘  │
└──────────────┼──────────────────────┘
               │
       ┌───────┴────────┐
       │                │
       ▼                ▼
┌─────────────┐  ┌──────────────┐
│  Supabase   │  │  OpenAI API  │
│ (PostgreSQL)│  │  (GPT-4,     │
│             │  │   Whisper)   │
└─────────────┘  └──────────────┘
```

---

## 🔄 Жизненный цикл Assessment

### Этапы:

```
1. СОЗДАНИЕ ASSESSMENT
   ↓
2. ОПРЕДЕЛЕНИЕ КОМПЕТЕНЦИЙ (через GPT)
   ↓
3. ГЕНЕРАЦИЯ ВОПРОСОВ (динамически, адаптивно)
   ↓
4. ОТВЕТ ПОЛЬЗОВАТЕЛЯ (голосом)
   ↓
5. ТРАНСКРИПЦИЯ (Whisper)
   ↓
6. ОЦЕНКА ОТВЕТА (GPT-4)
   ↓
7. ОБНОВЛЕНИЕ ПРОГРЕССА
   ↓
8. ЗАВЕРШЕНИЕ ТЕСТИРОВАНИЯ
   ↓
9. ГЕНЕРАЦИЯ ROADMAP
```

---

## 📝 Детальный процесс тестирования

### Этап 1: Создание Assessment

**Endpoint**: `POST /api/assessments`

**Входные данные:**

```json
{
  "direction": "backend(golang, sql)"
}
```

**Процесс:**

1. Извлечение `user_id` из JWT токена (поле `sub`)
2. Создание/получение пользователя в БД (`get_or_create_user`)
3. Определение компетенций через GPT-4 (`determine_competencies_by_direction`)
   - Модель: `gpt-4-turbo-preview`
   - Input: ~600 tokens
   - Output: ~1000 tokens
   - Стоимость: ~$0.036
4. Создание записи `assessment` в БД (статус: `in_progress`, `role_id`: NULL)
5. Создание записей `competency_assessment` для каждой компетенции
6. Поиск/создание компетенций в БД (`find_or_create_competency_by_name`)

**Результат:**

```json
{
  "assessment_id": "uuid",
  "competencies": [
    {
      "id": "uuid",
      "name": "Golang",
      "description": "...",
      "category": "Языки программирования"
    },
    ...
  ],
  "status": "in_progress"
}
```

---

### Этап 2: Генерация вопроса

**Endpoint**: `POST /api/questions/generate`

**Входные данные (Form Data):**

- `assessment_id`: UUID
- `competency_id`: UUID
- `question_number`: 1-7
- `difficulty`: 1-5 (опционально)

**Процесс:**

1. Проверка доступа к assessment
2. Получение контекста (`get_competency_assessment_context`):
   - Предыдущие ответы по этой компетенции
   - Выявленные пробелы в знаниях
   - Текущая сложность (из последнего `nextDifficulty` или среднее)
3. Определение сложности:
   - Если не указана - берется из контекста (по умолчанию 3)
   - Адаптируется на основе предыдущих ответов
4. Генерация вопроса через GPT-4 (`generate_question`):
   - Модель: `gpt-4-turbo-preview`
   - Input: ~800 tokens (system prompt + user prompt с контекстом)
   - Output: ~500 tokens (max_tokens=500)
   - Стоимость: ~$0.023 за вопрос
   - **Вопрос НЕ сохраняется в БД** (сохранится только после ответа)

**Промпт для генерации:**

- Учитывает предыдущие ответы
- Учитывает выявленные пробелы в знаниях
- Адаптирует сложность
- Генерирует вопрос для голосового ответа (1-3 минуты)

**Результат:**

```json
{
  "questionId": "temporary-uuid",
  "questionText": "Можете объяснить...",
  "difficulty": 3,
  "estimatedAnswerTime": "1-2 минуты",
  "expectedKeyPoints": ["ключевой момент 1", ...]
}
```

---

### Этап 3: Отправка ответа

**Endpoint**: `POST /api/questions/answer`

**Входные данные (Form Data):**

- `assessment_id`: UUID
- `competency_id`: UUID
- `question_text`: string (текст вопроса)
- `difficulty`: 1-5
- `audio`: File (webm, mp3, wav, m4a, ogg, макс. 25MB)

**Процесс:**

#### 3.1. Валидация

- Проверка формата файла
- Проверка размера (макс. 25MB)
- Проверка доступа к assessment

#### 3.2. Транскрипция аудио

- Модель: `whisper-1`
- Стоимость: ~$0.006 за минуту аудио
- Результат: текст транскрипции + метаданные

#### 3.3. Оценка ответа

- Модель: `gpt-4-turbo-preview`
- Input: ~1200 tokens (system prompt + вопрос + транскрипция)
- Output: ~2000 tokens (max_tokens=2000)
  - `score`: 1-5
  - `understandingDepth`: shallow/medium/deep
  - `isCorrect`: boolean
  - `feedback`: текст фидбека
  - `knowledgeGaps`: список пробелов
  - `nextDifficulty`: 1-5 (для следующего вопроса)
  - `reasoning`: объяснение оценки
  - `correctAnswer`: эталонный правильный ответ
  - `expectedKeyPoints`: ключевые моменты
- Стоимость: ~$0.072 за оценку

#### 3.4. Сохранение в БД

- Создание записи `question_history`:
  - `question_text`
  - `difficulty_level`
  - `user_answer_transcript`
  - `ai_evaluation` (полная оценка)
  - `is_correct`
  - `answered_at`
- Обновление `competency_assessment`:
  - `ai_assessed_score`: средний балл по всем ответам
  - `confidence_level`: low/medium/high (на основе количества ответов)
  - `gap_analysis`: все уникальные пробелы в знаниях
  - `test_session_data`: статистика

**Результат:**

```json
{
  "transcript": "Я использую useEffect для...",
  "evaluation": {
    "score": 4,
    "understandingDepth": "deep",
    "isCorrect": true,
    "feedback": "Хороший ответ...",
    "knowledgeGaps": [],
    "nextDifficulty": 4,
    "reasoning": "...",
    "correctAnswer": "Эталонный ответ...",
    "expectedKeyPoints": ["хуки", "жизненный цикл", ...]
  }
}
```

---

### Этап 4: Завершение тестирования

**Endpoint**: `POST /api/assessments/{assessment_id}/complete`

**Процесс:**

1. Вычисление общего балла (`calculate_overall_score`):
   - Взвешенное среднее по всем компетенциям
   - Учитывает `importance_weight` каждой компетенции
2. Обновление статуса на `completed`
3. Установка `completed_at`

**Формула общего балла:**

```
overall_score = Σ(score_i * weight_i) / Σ(weight_i)
```

---

### Этап 5: Генерация Roadmap

**Endpoint**: `GET /api/roadmaps/{assessment_id}`

**Процесс:**

1. Проверка, что assessment завершен
2. Генерация roadmap через GPT-4 (`generate_roadmap`):
   - Модель: `gpt-4-turbo-preview`
   - Input: ~2000 tokens (результаты тестирования)
   - Output: ~4000 tokens (max_tokens=4000)
   - Стоимость: ~$0.140
3. Сохранение roadmap в БД

**Структура roadmap:**

- Секции по компетенциям (приоритизированы по низким оценкам)
- Для каждой секции:
  - Материалы для изучения (статьи, видео, курсы)
  - Практические задачи
  - Вопросы для самопроверки

---

## 🔌 API Endpoints

### Assessments

| Метод  | Endpoint                         | Описание                           |
| ------ | -------------------------------- | ---------------------------------- |
| `POST` | `/api/assessments`               | Создать новое тестирование         |
| `GET`  | `/api/assessments/{id}`          | Получить информацию о тестировании |
| `POST` | `/api/assessments/{id}/complete` | Завершить тестирование             |

### Questions

| Метод  | Endpoint                  | Описание                        |
| ------ | ------------------------- | ------------------------------- |
| `POST` | `/api/questions/generate` | Сгенерировать адаптивный вопрос |
| `POST` | `/api/questions/answer`   | Отправить голосовой ответ       |

### Roadmaps

| Метод | Endpoint                        | Описание                       |
| ----- | ------------------------------- | ------------------------------ |
| `GET` | `/api/roadmaps/{assessment_id}` | Получить roadmap по assessment |

---

## 🤖 Использование OpenAI

### Модели и стоимость:

| Операция                | Модель              | Input tokens | Output tokens | Стоимость  |
| ----------------------- | ------------------- | ------------ | ------------- | ---------- |
| Определение компетенций | gpt-4-turbo-preview | ~600         | ~1000         | $0.036     |
| Генерация вопроса       | gpt-4-turbo-preview | ~800         | ~500          | $0.023     |
| Оценка ответа           | gpt-4-turbo-preview | ~1200        | ~2000         | $0.072     |
| Генерация roadmap       | gpt-4-turbo-preview | ~2000        | ~4000         | $0.140     |
| Транскрипция аудио      | whisper-1           | -            | -             | $0.006/мин |

### Общая стоимость одного теста:

- Определение компетенций: $0.036 (1 раз)
- Генерация вопросов: $0.161 (7 раз × $0.023)
- Оценка ответов: $0.504 (7 раз × $0.072)
- Транскрипция: $0.084 (7 раз × ~2 мин × $0.006)
- Roadmap: $0.140 (1 раз)

**Итого: ~$0.925 за полный тест**

---

## 💾 Структура данных

### Таблицы БД:

#### `assessments`

- `id`: UUID (PK)
- `user_id`: UUID (FK → users)
- `role_id`: UUID (FK → roles, может быть NULL)
- `status`: enum('in_progress', 'completed', 'abandoned')
- `overall_score`: float
- `started_at`: timestamp
- `completed_at`: timestamp

#### `competency_assessments`

- `id`: UUID (PK)
- `assessment_id`: UUID (FK → assessments)
- `competency_id`: UUID (FK → competencies)
- `ai_assessed_score`: int (1-5)
- `confidence_level`: enum('low', 'medium', 'high')
- `gap_analysis`: jsonb (пробелы в знаниях)
- `test_session_data`: jsonb (статистика)
- `completed_at`: timestamp

#### `question_history`

- `id`: UUID (PK)
- `competency_assessment_id`: UUID (FK → competency_assessments)
- `question_text`: text
- `difficulty_level`: int (1-5)
- `user_answer_transcript`: text
- `ai_evaluation`: jsonb (полная оценка от GPT)
- `is_correct`: boolean
- `asked_at`: timestamp
- `answered_at`: timestamp

#### `competencies`

- `id`: UUID (PK)
- `name`: string
- `description`: text
- `category`: string
- `importance_weight`: int

---

## ⚙️ Особенности реализации

### 1. Адаптивная генерация вопросов

**Ключевая фича**: Каждый вопрос генерируется на основе:

- Предыдущих ответов пользователя
- Выявленных пробелов в знаниях
- Текущего уровня сложности

**Алгоритм адаптации сложности:**

- Если ответ хороший (score 4-5) → увеличить сложность
- Если средний (score 3) → оставить ту же сложность
- Если слабый (score 1-2) → уменьшить сложность

### 2. Динамическое сохранение

**Вопросы НЕ сохраняются в БД до отправки ответа:**

- Вопрос генерируется динамически
- Сохраняется только после получения ответа
- Это позволяет адаптировать вопросы в реальном времени

### 3. Направление как текст

**Направление не хранится в БД:**

- Передается как текст (например, "backend(golang, sql)")
- Используется только для определения компетенций
- Компетенции определяются через GPT-4
- `role_id` в `assessments` может быть NULL

### 4. Автоматическое создание пользователей

**При создании assessment:**

- Если пользователя нет в БД - создается автоматически
- Используется `user_id` из JWT токена (поле `sub`)
- Email извлекается из JWT (если доступен)

### 5. Обработка ошибок

**Таймауты:**

- Таймаут для OpenAI API: 180 секунд (настраивается)
- Обработка `TimeoutError` с HTTP 504

**Квоты:**

- Обработка ошибки `insufficient_quota` с HTTP 402
- Понятные сообщения об ошибках

### 6. Валидация аудио

**Поддерживаемые форматы:**

- webm, mp3, wav, m4a, ogg

**Ограничения:**

- Максимальный размер: 25 MB
- Валидация на уровне API

---

## 📊 Статистика и метрики

### Confidence Level

Определяется на основе количества ответов:

- `low`: < 3 ответов
- `medium`: 3-4 ответа
- `high`: ≥ 5 ответов

### Общий балл

Вычисляется как взвешенное среднее:

```
overall_score = Σ(score_i × weight_i) / Σ(weight_i)
```

Где `weight_i` - это `importance_weight` компетенции.

---

## 🔐 Аутентификация

**JWT токены:**

- Передаются в заголовке `Authorization: Bearer {token}`
- `user_id` извлекается из поля `sub` в JWT
- `email` извлекается из поля `email` (если доступен)
- Валидация подписи временно отключена (для разработки)

---

## 🚀 Производительность

### Оптимизации:

1. **Кэширование** (частично):

   - OpenAI использует кэш для некоторых запросов (видно в дашборде)

2. **Асинхронность**:

   - Все операции асинхронные (async/await)
   - Параллельная обработка где возможно

3. **Таймауты**:
   - Настроены таймауты для предотвращения зависаний

---

## 📈 Будущие улучшения

1. **Оптимизация стоимости**:

   - Использование более дешевых моделей для простых задач
   - Уменьшение `max_tokens` где возможно
   - Кэширование повторяющихся запросов

2. **Улучшение адаптивности**:

   - Более точная оценка сложности
   - Учет времени ответа
   - Анализ паттернов ошибок

3. **Мониторинг**:
   - Логирование использования токенов
   - Метрики производительности
   - Аналитика использования

---

## 📝 Заключение

Система Assessment в Talim AI представляет собой **адаптивную AI-систему тестирования**, которая:

- ✅ Генерирует вопросы динамически на основе ответов пользователя
- ✅ Оценивает ответы через GPT-4 с выявлением пробелов
- ✅ Адаптирует сложность вопросов в реальном времени
- ✅ Генерирует персональные roadmaps для развития

**Основные преимущества:**

- Адаптивность - каждый тест уникален
- Точность - AI-оценка с детальным анализом
- Персонализация - roadmap под конкретного пользователя

**Основные затраты:**

- Оценка ответов: 54.5% от стоимости
- Генерация вопросов: 17.4%
- Roadmap: 15.1%

**Общая стоимость:** ~$0.925 за полный тест (7 вопросов по 7 компетенциям)
