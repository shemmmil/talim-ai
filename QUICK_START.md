# Quick Start Guide - Оптимизация данных

## ✅ Что сделано

### Проблема
При создании assessment мы **привязываем его к пользователю**, но текстовые транскрипты ответов **занимают много места** в БД.

### Решение
Теперь мы храним **только итоговый балл и оценки**, без полных текстовых транскриптов.

---

## 📊 Сравнение

### До оптимизации
```
1 assessment = 50 вопросов × 500 символов = 25KB
1000 пользователей × 3 попытки = 75MB только текста
```

### После оптимизации
```
1 assessment = 50 вопросов × 50 байт = 2.5KB
1000 пользователей × 3 попытки = 7.5MB
Экономия: 90% места! 🎉
```

---

## 🚀 Применить изменения

### Шаг 1: Backup базы данных
```bash
# В Supabase Dashboard: Database → Backups → Create backup
```

### Шаг 2: Применить миграцию
```sql
-- Скопировать содержимое файла и выполнить в SQL Editor:
-- database/migrations/optimize_question_history.sql
```

### Шаг 3: Перезапустить сервер
```bash
python -m app.main
```

### Шаг 4: Проверить
```bash
# Создать тестовый assessment
curl -X POST http://localhost:8000/api/assessments \
  -H "Authorization: Bearer test-user" \
  -H "Content-Type: application/json" \
  -d '{"direction": "frontend", "technology": "react"}'
```

---

## 📝 Что изменилось в коде

### ❌ Больше НЕ сохраняем:
- `user_answer_transcript` - полный текст ответа пользователя
- `audio_duration_seconds` - длительность аудио
- `transcription_confidence` - уверенность транскрипции
- `ai_evaluation` - большой JSONB объект (теперь разбит на поля)

### ✅ Сохраняем только:
- `score` (1-5) - итоговый балл
- `is_correct` (boolean) - правильно/неправильно
- `understanding_depth` (shallow/medium/deep) - глубина понимания
- `feedback` (text) - краткий фидбек (2-3 предложения)
- `knowledge_gaps` (array) - список пробелов в знаниях

---

## 🔍 Что хранится на каждом уровне

```
User (пользователь)
└── Assessments (попытки тестирования)
    ├── attempt_number: 1, 2, 3...
    ├── overall_score: 3.8
    └── Competency Assessments (оценки по компетенциям)
        ├── ai_assessed_score: 4
        ├── gap_analysis: {knowledgeGaps: [...]}
        └── Question History (детальная история)
            ├── question_text: "Вопрос..."
            ├── score: 4 ✅
            ├── is_correct: true ✅
            ├── feedback: "Хороший ответ" ✅
            ├── knowledge_gaps: [] ✅
            └── user_answer_transcript: ❌ НЕ ХРАНИМ
```

---

## 💡 Преимущества

### 1. Экономия места (90%)
- Меньше затрат на хранение
- Быстрее backup/restore
- Масштабирование дешевле

### 2. Приватность (GDPR)
- Не храним дословные ответы
- Только агрегированные данные
- Легче удалить данные пользователя

### 3. Производительность
- Быстрее запросы к БД
- Проще индексы
- Меньше трафика

### 4. Простота
- Структурированные поля вместо JSONB
- Легче делать аналитику
- Понятнее схема данных

---

## 🎯 Результат

Теперь для каждого assessment мы храним:
1. **Кто** (user_id) - пользователь
2. **Что** (direction + technology) - направление и технология
3. **Когда** (started_at, completed_at) - время прохождения
4. **Попытка** (attempt_number) - номер попытки
5. **Результат** (overall_score) - общий балл
6. **Детали** (scores по вопросам) - оценки без транскриптов

**Все что нужно для отслеживания прогресса, без лишних данных! 🚀**

---

## 📚 Дополнительно

- Полная документация: `docs/DATA_OPTIMIZATION.md`
- Руководство по миграции: `database/migrations/MIGRATION_GUIDE.md`
- Changelog: `CHANGELOG.md`

---

## 🆕 Новые возможности (v1.2.0)

### 1. Перезапуск assessment одним запросом
```bash
POST /api/assessments/{old_id}/restart
# Автоматически создает новый assessment с теми же параметрами
```

### 2. Оптимизированный каталог направлений
```bash
GET /api/catalog/directions?include_technologies=true
# Возвращает направления с технологиями в одном запросе!
```

### 3. Авто-завершение assessment
```bash
# При ответе на последний вопрос assessment автоматически завершается
POST /api/assessments/{id}/answers
# Ответ: {..., assessment_auto_completed: true, overall_score: 3.8}
```

Подробнее: `docs/API_V2_IMPROVEMENTS.md`

---

## ⚠️ Важно

После применения миграции **нельзя восстановить старые транскрипты**. 
Если нужны для анализа - сделайте backup ДО миграции!
